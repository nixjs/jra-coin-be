export enum BetStatus {
    COMMITTED = 1,
    BET,
    COMPLETED,
}

export enum BetWin {
    LOSE = 0,
    WIN,
}

export enum DiceDirection {
    ROLL_UNDER = 1,
    ROLL_OVER,
}

export enum GameSymbol {
    NONE,
    DICE,
    SLOT_MACHINE,
}
